name: Build and test

on:
  pull_request:
    branches: [ MCHRgithub ]
  push:
    branches: [ MCHRgithub ]

jobs:
  build-and-test:
    uses: GTNewHorizons/GTNH-Actions-Workflows/.github/workflows/build-and-test.yml@master
    secrets: inherit
